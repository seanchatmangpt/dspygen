version: 2

# Credentials are supplied through the NGROK_AUTHTOKEN environment variable.
# Never commit an ngrok authtoken to this repository.

tunnels:
  app:
    addr: 8000
    proto: http
    hostname: chatmangpt.ngrok.dev

  mqtt:
    addr: 1883
    proto: tcp
    remote_addr: 9.tcp.ngrok.io:24651
